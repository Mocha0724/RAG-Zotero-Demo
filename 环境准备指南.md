# RAG Demo 环境准备指南

## 必要条件

### 1. 硬件要求
- 至少4GB可用RAM
- 至少10GB可用磁盘空间（取决于Zotero文献库大小）
- 稳定的网络连接（用于访问DeepSeek API）

### 2. 软件要求
- **操作系统**: Windows 10/11, macOS, 或 Linux
- **Conda**: Miniconda或Anaconda最新版 [下载链接](https://docs.conda.io/en/latest/miniconda.html)
- **Python**: 3.8或更高版本（将通过Conda环境安装）
- **Node.js**: 14.0或更高版本（用于前端开发）
- **Git**: 最新版本

### 3. 必要账户和密钥
- **DeepSeek API密钥**: 需要注册[DeepSeek](https://platform.deepseek.com/)账户并获取API密钥
- **（可选）Hugging Face账户**: 如果需要使用开源嵌入模型

### 4. Zotero相关准备
- 安装Zotero软件 [下载链接](https://www.zotero.org/download/)
- 准备好需要作为知识库的Zotero文献库
- 熟悉Zotero的导出功能（支持BibTeX, CSV, RDF等格式）

## 安装步骤

### 1. 克隆项目仓库
```bash
git clone https://github.com/yourusername/RAG_Demo.git
cd RAG_Demo
```

### 2. 创建并激活Conda环境
```bash
conda create -n rag_demo python=3.10
conda activate rag_demo
```

### 3. 安装项目依赖
```bash
pip install -r requirements.txt
```

### 4. 安装前端依赖
```bash
cd app
npm install
cd ..
```

### 5. 环境变量配置
创建`.env`文件并配置以下环境变量：
```
DEEPSEEK_API_KEY=your_deepseek_api_key
EMBEDDING_MODEL=deepseek/text-embedding
VECTOR_DB_PATH=./data/embeddings
```

## 数据准备

### 1. Zotero数据导出
1. 打开Zotero应用
2. 选择需要导出的文献集合
3. 右键点击选择"导出集合"
4. 选择导出格式（推荐BibTeX或CSV+PDF附件）
5. 将导出的文件保存到项目的`data/raw`目录

### 2. PDF全文处理（可选）
如果导出的Zotero数据包含PDF文件，请确保它们与元数据文件一起存放在`data/raw`目录中，系统将自动处理这些PDF文件以提取全文内容。

## 验证安装

运行以下命令验证环境安装是否成功：
```bash
python scripts/check_environment.py
```

如果一切正常，您应该看到类似以下的输出：
```
环境检查通过!
Python版本: 3.10.x
DeepSeek API连接: 成功
必要依赖: 已安装
```

## 常见问题

### 1. API密钥访问问题
如果遇到DeepSeek API连接问题，请检查：
- API密钥是否正确配置
- 网络连接是否稳定
- 是否超出API使用限制

### 2. PDF处理问题
对于PDF处理错误，可能需要安装额外的系统依赖：
- Windows: 安装Visual C++ Redistributable
- Linux: `apt-get install poppler-utils tesseract-ocr`
- macOS: `brew install poppler tesseract`

### 3. 内存不足
如果处理大型文献库时遇到内存不足，请调整配置文件中的批处理大小或增加系统内存。 